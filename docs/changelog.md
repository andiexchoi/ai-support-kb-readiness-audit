# Changelog

Track decisions, scope changes, and methodological adjustments.

## 2026-05-31
- Scaffolded repo structure.

## 2026-06-01
- Locked project scope: KB-readiness audit (not RAG benchmark), 24 questions across 4 companies, 16–24 sources, 1–3 scoring scale, manual scoring with AI-assisted review.
- Rewrote CSV schemas for `support_kb_sources.csv`, `benchmark_questions.csv`, and `evaluation_results.csv` to add retrievability fields, the split between `public_kb_answerability_score` and `agent_safe_answerability_score`, AI answer scores, `evidence_type`, and `source_dependency_type`.
- Added `agent_boundary` block to `schemas/missing_delivery_ai_ready_content_model.yaml`.
- Updated rubric and scoring guide to the locked 1–3 dimensions.
- Updated methodology with calibration pass on 4 questions, retrievability test surface, paraphrased-summary policy, AI-assisted review boundary.
- Updated failure modes list.
- Scaffolded 4-question calibration pass: added `QCAL-001`–`QCAL-004` rows to `benchmark_questions.csv` and `evaluation_results.csv`, placeholder `DD-001`/`UB-001`/`LY-001`/`IC-001` rows to `support_kb_sources.csv`, and aligned the source-constrained answer prompt and unsupported-claims review prompt to the new ai_* scoring columns.
- Completed QCAL-001 (DoorDash missing delivery): filled `DD-001` in `support_kb_sources.csv`, wrote snapshot `data/raw/source_snapshots/doordash/DD-001.md`, populated benchmark and evaluation rows with manual scores. Surfaced two candidate rubric additions for the post-calibration revision — new failure mode `external_search_adds_unsupported_guidance` and an optional `external_search_behavior` notes field — both deferred until QCAL-002/003/004 are scored.
- Completed QCAL-002 (Uber driver no-show + charge): added two source rows (`UB-001` trip-not-taken refund page and `UB-002` cancellation fee page) to `support_kb_sources.csv`, wrote snapshots `data/raw/source_snapshots/uber/UB-001.md` and `UB-002.md`, populated benchmark and evaluation rows with `multi_article` source dependency and post-UB-002 scores (policy_clarity 3, evidence_type explicit_policy). Surfaced a third candidate failure mode for post-calibration: `ambiguous_issue_routing` (KB recognizes the query but resolution path branches across charge types — distinct from `hard_to_retrieve` and `scattered_across_multiple_pages`).
- Completed QCAL-003 (Lyft cancellation fee with non-moving driver): added two source rows (`LY-001` driver-facing audience-mismatch result and `LY-002` correct rider-facing policy) to `support_kb_sources.csv`, wrote snapshots `data/raw/source_snapshots/lyft/LY-001.md` and `LY-002.md`, populated benchmark and evaluation rows. Expected source = LY-002 (rider article appeared at rank 4; driver article ranked 1). Strong policy clarity and clear in-app dispute path, but the rider article does not address "driver not moving" as an explicit refund trigger. Surfaced a fourth candidate failure mode for post-calibration: `audience_mismatch_retrieval` (relevant content retrieved but for the wrong audience). Google AI Overview reinforced the `external_search_adds_unsupported_guidance` candidate by asserting Lyft "often refunds" GPS-stationary cases beyond what the rider article says.
- Completed QCAL-004 (Instacart missing grocery items): added two source rows (`IC-001` Refunds and returns; `IC-002` Report a problem with your order) to `support_kb_sources.csv`, wrote snapshots `data/raw/source_snapshots/instacart/IC-001.md` and `IC-002.md`, populated benchmark and evaluation rows. Exact natural-language search returned no articles; reformulated query "missing item" returned a small, highly relevant result set. Highest content quality of the four (public_kb_answerability 3, policy_clarity 3, customer_actionability 3, escalation_clarity 3, agent_safe_answerability 3). Failure mode = `hard_to_retrieve` (query phrasing sensitivity). Google AI Overview was mostly aligned but used stronger certainty than the source — a softer "overconfidence" variant of `external_search_adds_unsupported_guidance`.
- **4-question calibration pass complete.** Emerging typology across the four companies: DoorDash → hard_to_retrieve + outcome ambiguity; Uber → ambiguous_issue_routing; Lyft → audience_mismatch_retrieval; Instacart → query phrasing sensitivity (hard_to_retrieve) with strong content. Candidate post-calibration additions, all still deferred pending rubric revision discussion: failure modes `ambiguous_issue_routing`, `audience_mismatch_retrieval`, `external_search_adds_unsupported_guidance` (with overconfidence sub-variant); optional notes field `external_search_behavior`. AI answer dimensions (`ai_faithfulness_score`, `ai_completeness_score`, `ai_safety_score`, `ai_escalation_correctness_score`, `unsupported_claims`) intentionally left blank on all four calibration rows — AI answer review is the next calibration sub-step.
- Ran source-constrained AI answers for QCAL-001 – QCAL-004 using only captured source summaries; output saved to `analysis/calibration_ai_answers.md` in the 5-section format (customer answer, source-backed facts, missing info, escalation y/n, unsupported claims risk). No `ai_*` scores assigned yet — manual review pending. Pending post-calibration changes parked for after AI-answer review: (a) rename `staleness_risk_score` → `freshness_signal_score` with redefined 1–3 scale around freshness metadata exposure; (b) add column `external_search_behavior` to `evaluation_results.csv`; (c) add failure modes `query_phrasing_sensitivity`, `ambiguous_issue_routing`, `audience_mismatch_retrieval`, `external_search_adds_unsupported_guidance`, `external_search_overstates_policy_certainty`; (d) freeze rubric after these changes; (e) decide whether to rename `QCAL-001`–`QCAL-004` to `Q001`–`Q004` for the final dataset.
- Moved AI answer reviews to canonical location `data/ai_answer_reviews.csv` (removed empty stale `data/processed/ai_answer_reviews.csv`) and added rows for QCAL-001 – QCAL-004 with `ai_customer_facing_answer`, `source_backed_facts_used`, `missing_information`, `escalation_needed`, and `unsupported_claims_risk` populated from the source-constrained generation. The four `ai_*` score columns and `unsupported_claims` remain blank pending manual reviewer scoring. Updated `data/README.md` to point at the new path. LY-001 (driver-facing audience-mismatch result) intentionally excluded from the QCAL-003 answer set to avoid retrieval contamination.

## 2026-06-05
- **Rubric frozen** after the 4-question calibration pass. Applied the parked post-calibration changes: (a) renamed `staleness_risk_score` → `freshness_signal_score` in `rubric/evaluation_rubric.md`, `rubric/scoring_guide.md`, and `data/processed/evaluation_results.csv`, with a redefined 1–3 scale where higher = stronger visible freshness metadata (calibration rows remain at 1: none of DD-001, UB-001/002, LY-001/002, IC-001/002 exposed an article-level "last updated" date); (b) renamed `staleness_notes` → `freshness_notes` in `data/raw/support_kb_sources.csv` for naming consistency; (c) added column `external_search_behavior` to `evaluation_results.csv` and populated it for QCAL-001 – QCAL-004 from the previously-captured Google AI Overview observations; (d) added five new failure modes to `data/processed/failure_modes_summary.csv` — `query_phrasing_sensitivity`, `ambiguous_issue_routing`, `audience_mismatch_retrieval`, `external_search_adds_unsupported_guidance`, `external_search_overstates_policy_certainty` — and renamed `stale_or_undated_content` → `freshness_signal_absent`; (e) re-tagged calibration `failure_mode` values to use the new typology where applicable: QCAL-002 `scattered_across_multiple_pages` → `ambiguous_issue_routing`, QCAL-003 `scattered_across_multiple_pages` → `audience_mismatch_retrieval`, QCAL-004 `hard_to_retrieve` → `query_phrasing_sensitivity`; QCAL-001 stays `hard_to_retrieve`. Rubric is now locked — further changes require a new calibration pass.
- Folded the four calibration questions into the final 24-question benchmark. Renamed QCAL IDs to company-grouped final IDs:
  - QCAL-001 → Q001 (DoorDash)
  - QCAL-002 → Q007 (Uber)
  - QCAL-003 → Q013 (Lyft)
  - QCAL-004 → Q019 (Instacart)
  Final benchmark ID ranges: DoorDash Q001–Q006, Uber Q007–Q012, Lyft Q013–Q018, Instacart Q019–Q024. Renamed IDs across `data/processed/benchmark_questions.csv`, `data/processed/evaluation_results.csv`, `data/processed/failure_modes_summary.csv`, `data/ai_answer_reviews.csv`, `data/raw/source_snapshots/lyft/LY-001.md`, `LY-002.md`, `analysis/calibration_ai_answers.md`, and `rubric/evaluation_rubric.md`. Updated `methodology.md` to describe the calibration-to-final-benchmark fold. Calibration history preserved in this changelog and in `methodology.md`; final CSVs now use only Q001–Q024 IDs.
